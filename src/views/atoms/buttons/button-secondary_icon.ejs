<button 
  class="button-secondary_icon-<%= theme %>" 
  onClick="<%= action %>"
  aria-label="<%= text %>"
  title="<%= text %>"
  type="<%= type %>"
>
  <%= text %>
  <%- include(site.views + '/icons/user-interface/' + icon) %>
</button>