<button 
  class="button-icon-only icon-<%= icon %>" 
  onClick="<%= action %>"
  aria-label="<%= text %>"
  title="<%= text %>"
>
  <%- include(site.views + '/icons/user-interface/' + icon) %>
  <%= text %>
</button>