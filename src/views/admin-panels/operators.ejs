<pre>
  1. Get all users from Pimcore API
  2. Loop through users and display on screen

  eg:
  
  users.forEach(function(user, index) {
    include(site.views + '/molecules/user/', {index: index, user: user})
  });

</pre>
<ul>
  <% operators.forEach(function(operator, index) { %>
    <% if (operator.accountManager.UID === user.UID || user.usertype.slug === 'superAdmin') { %>
      <li>
        <%= operator.name %>
      </li>
    <% } %>
  <% }); %>
</ul>