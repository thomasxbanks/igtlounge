<%- contentFor('hero') %>
<section class="hero-text section-theme-light">
	<div class="grid grid-central">
		<div class="item-6-12">
				<h3 class="section-title"><%= site.page.body.title %></h3>
		</div>
		<hr class="page-divider" />
	</div>
</section>

	<%- contentFor('body') %>
		<form onSubmit="login()">
			<label>Username: <input type="text"></label>
			<label>Password: <input type="password"></label>
			<button type="submit">Log in</button>
		</form>
